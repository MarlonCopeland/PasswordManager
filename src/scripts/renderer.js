const electron = require('electron')
const ipc = electron.ipcRenderer

// var settingsEl = document.querySelector('.addPassword');
// settingsEl.addEventListener('click', function () {
//     ipc.send('open-addpassword-window');
// });